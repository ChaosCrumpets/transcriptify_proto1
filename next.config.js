/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  images: { unoptimized: true },
  serverActions: true, // This line enables Server Actions
};

module.exports = nextConfig;
